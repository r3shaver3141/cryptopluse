Onboarding: CryptoPluse Sign-Up Flow

1) User visits signup form on landing page (POST to /signup)
2) Server generates API key and returns JSON with key
3) Send welcome email (HTML + text templates) with key and quickstart
4) Deliver initial daily digest sample and price_index.json
5) Follow up with a short usage guide and programmatic examples
