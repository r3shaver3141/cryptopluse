API Gate Plan (CryptoPluse)\n\n- Choice: FastAPI (Python) MVP for rapid iteration; later options include Node/Express or Go.\n- Endpoints (MVP):\n  - GET /api/v1/price_index.json?api_key=KEY\n  - GET /api/v1/price_index.csv?api_key=KEY\n  - GET /api/v1/digests/daily.csv?api_key=KEY\n  - GET /api/v1/digests/weekly.csv?api_key=KEY\n- Auth: simple API keys in memory (persisted to memory/api_keys.json) with option to migrate to a DB\n- Rate limits: 1000 requests/day per key; bursts up to 10/min; token bucket\\n- Data: 7-coin snapshot (BTC, ETH, SOL, XRP, ADA, BNB, XLM) in memory/price_index.json + memory/price_index.csv, plus the daily digest CSV/JSON\n- Onboarding: sign-up flow, key provisioning, welcome packet (docs to follow)